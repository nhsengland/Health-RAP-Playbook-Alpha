    steps:
    # Calculates traffic and clones and stores in CSV file
    - name: Health RAP Playbook Repository Traffic 
      uses: sangonzal/repository-traffic-action@v.0.1.6
      env:
        TRAFFIC_ACTION_TOKEN: ${{ secrets.TRAFFIC_ACTION_TOKEN }} 